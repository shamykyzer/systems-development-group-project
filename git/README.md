# Git configuration

This folder holds canonical `.gitignore` files. To sync them to their target locations:

```bash
cp git/root.gitignore .gitignore
cp git/frontend.gitignore frontend/.gitignore
```

Or run from repo root: `./git/sync.sh`
